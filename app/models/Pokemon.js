import { Pop } from "../utils/Pop.js";


export class Pokemon {
  constructor(data) {
    
  }

  async getWildPokemons() {
    try {
      
    } catch (error) {
      Pop.error(error);
      console.error(error);
    }
  }
}